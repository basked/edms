 OPEN CLOSED
 Принцип отрытости закрытости: класс должен быть открыт для расширения,
 но закрыт для изменения новой функциональности.

  Когда для существующего класса необходимо внести новую функциональность,
  то необходимо реализовать с новый функционал в новом классе, а не изменять данный класс.

 Плюсы:
  1) Если брать компилируемые языки то проще откопилировать новый один класс а не весь проект.
  2) Если нам необходимо написать код для сохранения в облако - мы просто пишем  реализацию
     интерфейса Saiver и в нужном месте его вызываем.
  3) Таким образом был реализован шаблон проектирования "Стратегия".
     Мы выбираем стратегию сохранения данных отчета (либо в файл либо в БД).
  4) Так же есть возможность реализовать данный принцип через шаблон проектирования
      "Шаблонный метод" но там реализован через абсракный клас так как в в классе
      есть общее поведения.

 Пример: http://localhost/api/bas/solid/o

